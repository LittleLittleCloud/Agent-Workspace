{
  "name": "@agent-workspace/agent",
  "version": "0.1.0",
  "private": true,
  "description": "In-VM agent process for Agent Workspace sandbox",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.11.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.12.0"
  },
  "engines": { "node": ">=18" }
}
